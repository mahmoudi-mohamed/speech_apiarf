# Arabic TTS API

This project provides a Text-to-Speech (TTS) API for Arabic, using FastAPI and Hugging Face Transformers.

## How to run
1. `pip install -r requirements.txt`
2. `uvicorn main:app --reload`
